[pytest]
testpaths = tests
addopts =
    --cov=src
    --cov-report=term-missing
    --cov-fail-under=80
markers =
    integration: end-to-end pipeline tests that fit models and write artifacts
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
